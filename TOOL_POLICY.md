# Toolbox Tool Policy

本文件定义工具进入、扩展和保留在 Toolbox 中的审核标准。

核心原则：

> **项目和真实工作流是主线；工具用于降低重复劳动、错误率和上下文成本。不要为了扩充 Toolbox 而造工具。**

## 1. 工具准入四问

新增工具或显著扩展现有工具前，逐项回答：

1. **真实工作流**：它是否直接服务当前项目、科研任务或已经反复出现的个人工作流？
2. **重复痛点**：它解决的问题是否已经重复出现，或有充分理由判断会持续重复？
3. **可复用性**：首次实现之后，是否预计至少还能复用 2 次，而不是一次性任务？
4. **杠杆价值**：它是否能明显减少人工时间、出错概率、上下文消耗或结果不一致？

### 判定

| 通过项 | 处理方式 |
|---|---|
| 4/4 | 可以作为正式 Toolbox 工具建设和维护 |
| 3/4 | 可以进入 Toolbox，但先保持最小实现；缺失项需要有明确理由 |
| 2/4 | 优先保留为项目内脚本或临时脚本，不继续平台化 |
| 0–1/4 | 不建议建设 |

“有趣”“看起来高级”“以后也许有用”不算通过理由。

## 2. 功能扩展审核

一个工具已经存在，不代表所有新功能都值得加入。每个显著新功能至少应满足以下一项，并且不能明显破坏工具边界：

- 对应一次真实使用中已经发生的失败或高频摩擦；
- 会被多个现有任务复用；
- 明显提高正确性、可检查性或可恢复性；
- 明显减少重复人工操作。

以下理由本身不足以支持扩展：

- 为了“更通用”预先增加抽象层；
- 为了“更智能”引入 Agent、自动评分或复杂决策图；
- 为了“更完整”加入尚无真实调用者的配置项；
- 因为未来可能需要，就提前实现 diff、缓存、插件系统、数据库或服务化。

## 3. 复杂度预算

复杂度只有在解决真实复杂问题时才合理。

判断实现是否过度工程化时，不按代码行数，而按以下问题判断：

- 每一块复杂逻辑能否对应具体失败模式、平台差异或稳定接口需求？
- 删除这块逻辑后，是否会恢复一个已经发生过的问题，或明显降低可靠性？
- 是否存在更简单、同样可靠的实现？

如果复杂度主要来自问题本身，可以保留；如果复杂度主要来自预想中的未来需求，应推迟。

## 4. 生命周期

### Incubate

首次出现的问题先用临时脚本、配置或项目内实现解决，不急于进入 Toolbox。

### Promote

当问题重复出现并通过“工具准入四问”后，再整理为独立工具：

- 明确输入与输出；
- 提供最小 README；
- 显式错误处理；
- 至少有 smoke test、最小示例或真实验证记录；
- 不写入具体课题绝对路径和一次性真值。

### Stabilize

工具已经解决问题后，优先稳定接口和维护，而不是继续堆功能。

### Freeze / Remove

若工具长期没有真实复用、已经被更简单方案替代，或维护成本超过收益，应停止扩展；必要时移出 Toolbox，而不是因为已经写过就永久保留。

## 5. 当前工具审计

本次按“工具准入四问”审计现有工具。结论关注是否值得继续作为 Toolbox 工具存在，不等同于完整代码质量审计。

| 工具 | 真实工作流 | 重复痛点 | 可复用性 | 杠杆价值 | 结论 | 当前约束 |
|---|---:|---:|---:|---:|---|---|
| `academic_plot/` | ✓ | ✓ | ✓ | ✓ | **4/4，保留并稳定** | 两个绘图脚本存在少量重复逻辑；在只有两种模式时不值得为了抽象而拆公共模块。只有出现第 3 种绘图模式或重复修改开始造成维护成本时再抽取公共层。 |
| `conversation_pack/` | ✓ | ✓ | ✓ | ✓ | **4/4，继续试验但停止无证据扩展** | 当前 v0.2 的 Preview → 用户筛选 → deterministic build 边界合理。先用真实长对话验证；不要提前增加自动评分、完整聊天存档、节点图或复杂 Agent 编排。 |
| `file_scan/` | ✓ | ✓ | ✓ | ✓ | **4/4，保留并稳定** | 当前 summary/tree/files/manifest 已覆盖核心目标。暂不增加增量扫描、diff、数据库或向量索引，除非真实目录工作流反复证明需要。 |
| `plot_digitizer_launcher/` | ✓ | ✓ | ✓ | ✓ | **4/4，保留但不泛化** | 虽然是单一软件专用封装，但启动、构建、健康检查和安全停止均对应真实 Windows 摩擦与误杀风险。保持 PlotDigitizer 专用，不抽象成通用“进程启动框架”。 |

## 6. 当前审计说明

### `academic_plot/`

它把科研验证图中反复出现的视觉语法、QC、字体、SVG 输出和 reference/simulation 语义固定下来，属于典型的“重复科研任务 → 稳定工具”。现有两个脚本分别服务“多站点 × 单变量”和“单事件 × 多变量”，数据组织不同，因此目前保持两个单文件脚本比提前做继承/插件架构更合理。

### `conversation_pack/`

它解决长对话迁移时反复出现的状态丢失、附件遗漏和上下文膨胀问题。当前将语义判断留给 GPT/用户，将复制、hash、manifest、QA 和 ZIP 留给确定性脚本，职责边界清楚。风险不在现有实现，而在未来为了“更智能”继续扩张。

### `file_scan/`

它解决大目录直接喂给 AI 时上下文成本高、文件难定位的问题。summary → tree → files.csv → manifest 的分层读取顺序有明确使用者和明确收益。v2.0 已经足够完整，下一阶段应以真实使用验证为主，而不是继续增加能力面。

### `plot_digitizer_launcher/`

它表面上只是启动器，但实现复杂度主要对应 Windows 环境差异、前端重建判断、服务健康检查、PID 复用和误杀风险。这属于“问题本身产生的必要复杂度”。目前不需要把它泛化到其他软件。

## 7. 默认维护规则

以后新增工具或显著扩展现有工具时，先在 PR、提交说明或对话中回答“工具准入四问”。

如果答案不足 3/4，默认先不要进入 Toolbox；如果已经存在，默认不要继续扩展。
